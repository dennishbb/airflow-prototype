# Airflow Prototype

This repo contains:
- Apache Airflow service (Docker + Compose)
- Jenkins Multibranch Pipeline
- EnterpriseSharedLibrary integration

## Local run

```bash
docker compose up --build

